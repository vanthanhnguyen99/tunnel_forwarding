FROM python:3.12-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends openssh-client \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY tunnel_admin /app/tunnel_admin

ENTRYPOINT ["python", "-m", "tunnel_admin.worker"]
